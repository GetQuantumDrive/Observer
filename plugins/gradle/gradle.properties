group=io.getquantumdrive
# version is set by CI from /VERSION; override here for local development
version=0.1.0-SNAPSHOT

# Gradle Plugin Portal credentials (set via environment or ~/.gradle/gradle.properties)
# gradle.publish.key=
# gradle.publish.secret=
