package io.getquantumdrive.observer

/** The observer CLI version this plugin was built to use. Kept in sync with /VERSION at release time. */
object PluginVersion {
    const val VERSION = "0.1.0"
}
